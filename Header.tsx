'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {Menu,X,ArrowUpRight} from 'lucide-react';
export default function Header(){const[open,setOpen]=useState(false);useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow=''}},[open]);const links=[['Home','#home'],['Services','#services'],['About','#about'],['Portfolio','#portfolio'],['Testimonials','#testimonials'],['FAQ','#faq'],['Contact','#contact']];return <header className="site-header"><div className="container nav"><Link href="/" className="logo" onClick={()=>setOpen(false)} aria-label="ShashikDigital home"><span className="logo-mark">SD</span><span>Shashik<span>Digital</span></span></Link><nav className="desktop-nav" aria-label="Primary">{links.map(([l,h])=><Link key={l} href={h}>{l}</Link>)}</nav><Link href="/#contact" className="btn btn-primary nav-cta">Get a Free Quote <ArrowUpRight size={16}/></Link><button className="menu-btn" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label={open?'Close menu':'Open menu'}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="mobile-menu"><nav aria-label="Mobile">{links.map(([l,h])=><Link key={l} href={h} onClick={()=>setOpen(false)}>{l}</Link>)}</nav><Link href="/#contact" className="btn btn-primary" onClick={()=>setOpen(false)}>Get a Free Quote <ArrowUpRight size={16}/></Link></div>}</header>}
