'use client';
import {useState} from 'react'; import Link from 'next/link'; import {ArrowRight,ArrowUpRight,Check,ChevronLeft,ChevronRight,ExternalLink,MessageCircle,X,Monitor,TrendingUp,Search,Share2,Palette,ShoppingBag} from 'lucide-react'; import {services,portfolio,faqs,contact,gallery,testimonials} from '@/lib/data'; import ContactForm from './ContactForm';
const wa=`https://wa.me/91${contact.whatsapp}?text=${encodeURIComponent('Hello ShashikDigital, I would like to discuss a project.')}`; const iconMap={Monitor,TrendingUp,Search,Share2,Palette,ShoppingBag};
export default function Site(){const[filter,setFilter]=useState('All');const[faq,setFaq]=useState<number|null>(null);const[light,setLight]=useState<number|null>(null);const[gLight,setGLight]=useState<number|null>(null);const filtered=filter==='All'?portfolio:portfolio.filter(x=>x.cat===filter);return <>
<section id="home" className="hero grid-bg"><div className="glow glow-a"/><div className="container hero-grid"><div className="reveal"><div className="eyebrow">DIGITAL STRATEGY · DESIGN · TECHNOLOGY</div><h1 className="display hero-title">Build a digital presence that <span>moves your business forward.</span></h1><p className="hero-copy">ShashikDigital creates thoughtful websites, brands and digital experiences designed to make businesses look credible, connect with customers and grow with confidence.</p><div className="hero-actions"><Link href="#contact" className="btn btn-primary">Get a Free Quote <ArrowUpRight size={17}/></Link><Link href="#portfolio" className="btn">View Our Work</Link><a href={wa} className="btn"><MessageCircle size={17}/> WhatsApp Us</a></div><div className="proof-row"><span><Check/> Responsive experiences</span><span><Check/> Transparent process</span><span><Check/> Client-focused delivery</span></div></div><HeroVisual/></div></section>
<section className="trust-strip"><div className="container trust-grid"><Trust t="Professional Digital Solutions"/><Trust t="Responsive Experiences"/><Trust t="Transparent Process"/><Trust t="Client-Focused Approach"/></div></section>
<section id="services" className="section"><div className="container"><Heading eyebrow="What we do" title="Everything you need to grow digitally" text="A focused set of services that can work independently or as one connected digital system."/><div className="service-grid">{services.map((s,i)=>{const Icon=iconMap[s.icon as keyof typeof iconMap];return <article key={s.title} className="card service-card"><div className="service-top"><div className="icon-box"><Icon size={21}/></div><span>0{i+1}</span></div><h3 className="display">{s.title}</h3><p>{s.desc}</p><ul>{s.benefits.map(b=><li key={b}><Check size={15}/>{b}</li>)}</ul><Link href="#contact">Discuss this service <ArrowRight size={15}/></Link></article>})}</div></div></section>
<section className="section section-tint"><div className="container why-grid"><div><div className="eyebrow">WHY US</div><h2 className="display section-title">Why choose ShashikDigital?</h2><p>We combine strategy, aesthetics and dependable execution without unnecessary complexity.</p><div className="mini-proof"><strong>01</strong><span>Clear communication from first conversation to launch.</span></div></div><div className="reason-list">{['Strategy First','Custom Solutions','Modern Technology','Responsive Design','Transparent Communication','Long-Term Support'].map((x,i)=><div className="reason" key={x}><b>0{i+1}</b><div><h3>{x}</h3><p>A practical principle used to keep the work clear, useful and aligned with the project goal.</p></div></div>)}</div></div></section>
<section className="section"><div className="container"><Heading eyebrow="THE PROCESS" title="From idea to launch, with clarity at every step."/><div className="process-grid">{[['Discovery','Understand the business, audience and goals.'],['Strategy','Define the right approach and priorities.'],['Design','Create the visual experience and content structure.'],['Development','Build the responsive website or digital solution.'],['Testing','Check functionality, responsiveness and performance.'],['Launch','Prepare the final solution for launch.']].map(([x,d],i)=><div className="card process-card" key={x}><span>0{i+1}</span><h3 className="display">{x}</h3><p>{d}</p></div>)}</div></div></section>
<section id="portfolio" className="section section-tint"><div className="container"><Heading eyebrow="SELECTED WORK" title="Our work" text="Demo concepts are intentionally labelled. Replace them with real projects before publishing."/><div className="filter-row">{['All','Web Design','Web Development','Branding','Social Media','Marketing','E-commerce'].map(f=><button key={f} onClick={()=>setFilter(f)} className={'btn filter '+(filter===f?'active':'')}>{f}</button>)}</div><div className="portfolio-grid">{filtered.map((p,i)=><article className="portfolio-card" key={p.name}><button onClick={()=>setLight(i)} aria-label={`Preview ${p.name}`}><div className="portfolio-art"><div className="mock-window"><div className="mock-bar"/><div className="mock-hero"/><div className="mock-cols"><i/><i/><i/></div></div><span>DEMO PLACEHOLDER</span></div><div className="portfolio-info"><small>{p.cat}</small><h3 className="display">{p.name}</h3><p>{p.desc}</p><em>{p.tags}</em><div className="project-link">View Project <ArrowUpRight size={15}/></div></div></button></article>)}</div></div></section>
<section className="section"><div className="container case-study"><div><div className="eyebrow">CASE STUDY FORMAT</div><h2 className="display section-title">Every project starts with the problem, not the pixels.</h2><p>When real case studies are added, this structure keeps the story useful and credible.</p></div><div className="case-grid">{[['Challenge','What needed to improve?'],['Approach','What strategy was selected?'],['Solution','What was designed and built?'],['Result','Only verified outcomes are shown.']].map(([a,b])=><div className="card" key={a}><span>{a}</span><h3>{b}</h3></div>)}</div></div></section>
<section id="about" className="section section-tint"><div className="container about-grid"><div className="about-art card grid-bg"><div className="sd-logo">SD<span>.</span></div><div className="value-grid">{['Quality','Creativity','Transparency','Performance','Customer Focus'].map(v=><span key={v}>{v}</span>)}</div></div><div><div className="eyebrow">ABOUT SHASHIKDIGITAL</div><h2 className="display section-title">Digital work should feel as good as it performs.</h2><p>ShashikDigital is a digital services brand focused on creating professional, modern and useful experiences for businesses.</p><p>Our approach is simple: understand the goal, make the experience clear, execute carefully and communicate transparently.</p><div className="tag-row"><span>Quality</span><span>Creativity</span><span>Transparency</span><span>Performance</span><span>Customer Focus</span></div><p className="editable-note">Founder/company story, credentials and team details can be added here when officially supplied.</p></div></div></section>
<section id="testimonials" className="section"><div className="container"><Heading eyebrow="CLIENT VOICE" title="Testimonials" text="Real testimonials will be displayed here once supplied. No client quote is fabricated."/><div className="testimonial card"><div className="quote-mark">“</div><blockquote>{testimonials[0].quote}</blockquote><footer>{testimonials[0].name} · {testimonials[0].company} · {testimonials[0].service}</footer><div className="slider-controls"><button aria-label="Previous testimonial" disabled><ChevronLeft/></button><span>01 / 01</span><button aria-label="Next testimonial" disabled><ChevronRight/></button></div></div></div></section>
<section className="section section-tint" id="gallery"><div className="container"><Heading eyebrow="VISUAL GALLERY" title="A gallery for your best digital work" text="Replace demo tiles with your real website, branding, creative and social work."/><div className="gallery-grid">{gallery.map((g,i)=><button className="gallery-tile" key={i} onClick={()=>setGLight(i)} aria-label={`Open ${g.title}`}><div className="gallery-art"><div className="gallery-shape"/></div><div><small>{g.cat}</small><strong>{g.title}</strong></div></button>)}</div></div></section>
<section id="pricing" className="section"><div className="container pricing-card card"><div><div className="eyebrow">CUSTOM PROJECTS</div><h2 className="display section-title">Need a custom solution?</h2><p>Tell us what you need and we'll prepare a tailored quote. No invented packages or prices.</p></div><Link href="#contact" className="btn btn-primary">Request a Quote <ArrowUpRight size={17}/></Link></div></section>
<section id="faq" className="section section-tint"><div className="container faq-grid"><div><div className="eyebrow">FAQ</div><h2 className="display section-title">Clear answers before you start.</h2><p>If your question is not here, send an enquiry and we can discuss your project directly.</p></div><div className="faq-list">{faqs.map(([q,a],i)=><div className="card faq-item" key={q}><button onClick={()=>setFaq(faq===i?null:i)} aria-expanded={faq===i}><span>{q}</span><span>{faq===i?'−':'+'}</span></button>{faq===i&&<div className="faq-answer">{a}</div>}</div>)}</div></div></section>
<section className="section final-cta"><div className="container cta-box"><div className="eyebrow">LET'S BUILD</div><h2 className="display">Ready to build something better?</h2><p>Tell us about your project and let's explore how ShashikDigital can help.</p><div className="hero-actions"><Link href="#contact" className="btn btn-primary">Get a Free Quote <ArrowUpRight size={17}/></Link><a href={wa} className="btn"><MessageCircle size={17}/> Talk to Us on WhatsApp</a></div></div></section>
<section id="contact" className="section contact-section"><div className="container contact-grid"><div><div className="eyebrow">CONTACT</div><h2 className="display section-title">Let's talk about your next project.</h2><p>Share a few details and we'll have a clearer starting point for the conversation.</p><div className="contact-details"><a href={`tel:+91${contact.phone}`}><b>Phone</b><span>+91 {contact.phone}</span></a><a href={`mailto:${contact.email}`}><b>Email</b><span>{contact.email}</span></a><a href={`mailto:${contact.secondaryEmail}`}><b>Alternate Email</b><span>{contact.secondaryEmail}</span></a><a href={wa}><b>WhatsApp</b><span>+91 {contact.whatsapp}</span></a><div><b>Address</b><span>{contact.address}</span></div></div></div><ContactForm/></div></section>
{light!==null&&<Lightbox title={filtered[light]?.name||''} cat={filtered[light]?.cat||''} onClose={()=>setLight(null)} onPrev={()=>setLight((light-1+filtered.length)%filtered.length)} onNext={()=>setLight((light+1)%filtered.length)}/>} {gLight!==null&&<Lightbox title={gallery[gLight].title} cat={gallery[gLight].cat} onClose={()=>setGLight(null)} onPrev={()=>setGLight((gLight-1+gallery.length)%gallery.length)} onNext={()=>setGLight((gLight+1)%gallery.length)}/>}<a href={wa} aria-label="Chat on WhatsApp" className="wa-float"><MessageCircle/></a></>}
function Lightbox({title,cat,onClose,onPrev,onNext}:{title:string;cat:string;onClose:()=>void;onPrev:()=>void;onNext:()=>void}){return <div className="lightbox" role="dialog" aria-modal="true" aria-label="Project preview"><button className="light-close" onClick={onClose} aria-label="Close"><X/></button><button className="light-prev" onClick={onPrev} aria-label="Previous"><ChevronLeft/></button><div className="light-content"><small>{cat}</small><h3 className="display">{title}</h3><div className="light-art grid-bg"><div className="mock-window"><div className="mock-bar"/><div className="mock-hero"/><div className="mock-cols"><i/><i/><i/></div></div></div><p>Demo placeholder — replace with the real project visual and details before publishing.</p></div><button className="light-next" onClick={onNext} aria-label="Next"><ChevronRight/></button></div>}
function HeroVisual(){return <div className="hero-visual reveal"><div className="hero-device grid-bg"><div className="dashboard"><div className="dash-top"><span/><i/></div><small>DIGITAL EXPERIENCE</small><div className="dash-title"/><div className="dash-line"/><div className="dash-cards"><i/><i/><i/></div></div><div className="float-card one"><small>PROJECT STATUS</small><b>Ready for the next idea</b></div><div className="float-card two"><small>UX / UI</small><b>Clear · Fast · Responsive</b></div></div></div>}
function Trust({t}:{t:string}){return <div><Check size={17}/><span>{t}</span></div>}; function Heading({eyebrow,title,text}:{eyebrow:string;title:string;text?:string}){return <div className="heading"><div className="eyebrow">{eyebrow}</div><h2 className="display section-title">{title}</h2>{text&&<p>{text}</p>}</div>}
